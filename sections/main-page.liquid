{%- comment -%} Generic content page: contact, privacy policy, terms. {%- endcomment -%}
<section class="section">
  <div class="wrap wrap--narrow">
    <h1 class="h2">{{ page.title }}</h1>
    <div class="rte">{{ page.content }}</div>
  </div>
</section>
{% schema %}
{ "name": "Page content" }
{% endschema %}
